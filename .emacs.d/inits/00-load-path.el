;;; -*- coding: utf-8; mode: emacs-lisp; -*-
;;; 000load-path.el ---
;;
;;
;; load-pathの設定
;; ----------------------------------------------------------

; auto-install, el-get 以外の手段でインストールした elips
(add-to-list 'load-path (expand-file-name "~/.emacs.d/elisp/manual-install"))

